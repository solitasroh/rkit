name: "cmake-embedded"
classification: "capability"
version: "2.0.0"
evals:
  - name: "cortex-m4-toolchain-build"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce CMakeLists.txt configuration output"
      - "Must follow cross-compile toolchain setup process steps"
    timeout: 60000
