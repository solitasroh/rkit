name: "communication"
classification: "capability"
version: "2.0.0"
evals:
  - name: "multi-protocol-dma-driver"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce communication driver code implementation"
      - "Must follow DMA/interrupt protocol process steps"
    timeout: 60000
