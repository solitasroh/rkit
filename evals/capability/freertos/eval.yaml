name: "freertos"
classification: "capability"
version: "2.0.0"
evals:
  - name: "sensor-task-queue-design"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce task/queue code implementation"
      - "Must follow RTOS task design process steps"
      - "No common stack overflow pitfalls"
    timeout: 60000
