name: "imx-bsp"
classification: "capability"
version: "2.0.0"
evals:
  - name: "uart-i2c-device-tree"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce Device Tree node configuration output"
      - "Must follow BSP porting process steps"
    timeout: 60000
