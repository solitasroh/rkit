name: "kernel-driver"
classification: "capability"
version: "2.0.0"
evals:
  - name: "platform-driver-spi-sensor"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce kernel module code implementation"
      - "Must follow driver probe/remove process steps"
    timeout: 60000
