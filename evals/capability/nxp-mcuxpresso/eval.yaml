name: "nxp-mcuxpresso"
classification: "capability"
version: "2.0.0"
evals:
  - name: "gpio-spi-driver-init"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce fsl_* API code implementation"
      - "Must follow SDK initialization setup steps"
    timeout: 60000
