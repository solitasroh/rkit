name: "serial-bridge"
classification: "capability"
version: "2.0.0"
evals:
  - name: "mcu-wpf-packet-protocol"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce serial bridge protocol code implementation"
      - "Must follow MCU-Desktop communication bridge design process steps"
    timeout: 60000
