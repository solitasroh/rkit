name: "stm32-hal"
classification: "capability"
version: "2.0.0"
evals:
  - name: "uart-dma-circular-buffer"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce HAL API code implementation"
      - "Must follow DMA/interrupt process steps"
      - "No common STM32 safety pitfalls"
    timeout: 60000
