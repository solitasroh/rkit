name: "wpf-mvvm"
classification: "capability"
version: "2.0.0"
evals:
  - name: "sensor-viewmodel-toolkit"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce ViewModel code implementation"
      - "Must follow MVVM data binding pattern design steps"
      - "No common WPF binding pitfalls"
    timeout: 60000
