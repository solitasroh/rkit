name: "xaml-design"
classification: "capability"
version: "2.0.0"
evals:
  - name: "sensor-data-template-style"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce XAML style/template output"
      - "Must follow ResourceDictionary design pattern steps"
    timeout: 60000
