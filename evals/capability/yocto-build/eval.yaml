name: "yocto-build"
classification: "capability"
version: "2.0.0"
evals:
  - name: "custom-recipe-systemd"
    prompt: "prompt-1.md"
    expected: "expected-1.md"
    criteria:
      - "Must produce recipe configuration output"
      - "Must follow Yocto build setup process steps"
    timeout: 60000
